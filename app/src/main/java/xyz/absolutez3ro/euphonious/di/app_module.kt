package xyz.absolutez3ro.euphonious.di

import org.koin.core.module.Module
import org.koin.dsl.module

val groupModule: Module = module {
    //    viewModel { SongViewModel(androidApplication().contentResolver) }
}